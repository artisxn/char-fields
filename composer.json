{
    "name": "codicastudio/nova-charcounted-fields",
    "description": "A noobs worthless garbage, just learning.",
    "keywords": [
        "worthless garabage by a noob."
    ],
    "license": "MIT",
    "require": {
        "php": "^7.4.0"
    },
    "autoload": {
        "psr-4": {
            "codicastudio\\CharcountedFields\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "codicastudio\\CharcountedFields\\FieldServiceProvider"
            ]
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "test-coverage": "vendor/bin/phpunit --coverage-html coverage"
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
